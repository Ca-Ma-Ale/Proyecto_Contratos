/**
 * Formateo de vista únicamente - Versión simplificada
 * Este archivo solo se ejecuta en modo vista (no edición)
 * Delegando todo el trabajo de edición a formatMiles.js y format_edit_view.js
 */

document.addEventListener('DOMContentLoaded', function() {
    // Este archivo ya no hace nada automáticamente
    // Todo el formateo está manejado por:
    // - format_edit_view.js (carga inicial en modo edición)
    // - formatMiles.js (interacción del usuario)
});

